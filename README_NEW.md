# DeFi Dashboard - Тестовое задание ✨

Полнофункциональное DeFi приложение для управления портфелем, отслеживания прибыли/убытков и управления позициями.

## 🎯 Быстрый старт

```bash
# 1. Установить зависимости
npm install

# 2. Убедиться что .env.local содержит правильные значения
# Используйте .env.local из проекта

# 3. Запустить dev сервер
npm run dev

# 4. Открыть http://localhost:3000 в браузере
```

## ✨ Основные возможности

### 2 основных блока (как в дизайне)

- **My Wallet блок (левый)**
  - Иконка кошелька с оранжевым градиентом
  - Название "My Wallet" и дата присоединения
  - Показатель Portfolio (Not USDC)
  - Крупный баланс в WETH
  - Изменение за день с цветом (зелёный/красный)
  - Кнопки Deposit и Withdraw с Motion анимациями

- **Profit/Loss блок (правый)**
  - Заголовок "Profit/Loss" с иконкой
  - Временные фильтры: 1H, 6H, 1D, 1W, 1M, All
  - Крупное число с P&L (+/- с цветом)
  - День недели при наведении
  - Интерактивный Area Chart с оранжевой заливкой
  - При наведении на график обновляется значение выше

### Анимации (Motion)

- **whileHover** - кнопки увеличиваются при наведении
- **whileDrag** - уменьшаются при перетаскивании
- **whileTap** - сжимаются при клике
- **NumberFlow** - красивая анимация чисел
- Плавные transitions при загрузке компонентов

### Функционал

- ✅ Deposit транзакции (ServerAction)
- ✅ Withdraw транзакции (ServerAction)
- ✅ Отслеживание P&L по позициям
- ✅ История транзакций с EtherScan
- ✅ Статистика портфеля (Total, 24h Change, High, Low)
- ✅ Кеширование на 1 минуту (привязано к publicKey)

## 📋 Технологический стек

```
Next.js 14 + React 18 + TypeScript 5
├── Framer Motion 11 (whileHover, whileDrag, whileTap анимации)
├── NumberFlow (анимация чисел)
├── Recharts 2 (Area Chart для графика)
├── Ethers.js 6 (работа с блокчейном)
├── Tailwind CSS 3 (стили)
└── React Hot Toast 2 (уведомления)
```

## 🏗️ Архитектура

```
src/app/
├── page.tsx                    # 📌 Server Component (главная страница)
├── layout.tsx                  # 🎯 Root layout с Tailwind
├── globals.css                 # 🎨 Global styles (@tailwind)
│
├── actions/                    # 🔐 Server Actions (все запросы на сервер)
│   ├── wallet.actions.ts       # Deposit, Withdraw, getWalletBalance
│   ├── chart.actions.ts        # getChartData (с кешированием)
│   └── positions.actions.ts    # getPositionPnL
│
├── components/                 # 🎭 Client Components (UI)
│   ├── BalanceCard.tsx         # 💰 Левый блок - My Wallet
│   ├── ChartBlock.tsx          # 📈 Правый блок - Profit/Loss
│   ├── ChartView.tsx           # 📊 Area Chart
│   ├── MotionButton.tsx        # 🎬 Button с Motion
│   ├── TimeframeSelector.tsx   # ⏱️ Timeframe фильтры
│   ├── PortfolioStats.tsx      # 📉 Статистика
│   ├── TransactionHistory.tsx  # 💸 История транзакций
│   ├── PageHeader.tsx          # 🎀 Заголовок
│   ├── ChartSkeleton.tsx       # ⚫ Loading skeleton
│   └── ToasterProvider.tsx     # 🔔 Toast уведомления
│
├── lib/                        # 🛠️ Утилиты
│   ├── etherscan.ts            # API EtherScan
│   ├── wallet.ts               # Работа с кошельком
│   ├── cache.ts                # Server-side кеширование
│   └── ...
│
└── types/
    └── chart.ts                # TypeScript типы
```

## 🔐 Переменные окружения

```env
# RPC / Chain
RPC_URL=https://mainnet.infura.io/v3/YOUR_KEY
NEXT_PUBLIC_CHAIN_ID=1

# Wallet (SERVER ONLY)
WALLET_PRIVATE_KEY=0x...
PUBLIC_WALLET_ADDRESS=0x...

# Token (NOT USDC)
TOKEN_ADDRESS=0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2
TOKEN_DECIMALS=18
TOKEN_SYMBOL=WETH

# Etherscan
ETHERSCAN_API_KEY=YOUR_KEY

# App Settings
NEXT_PUBLIC_CHAIN_EXPLORER=https://etherscan.io
```

## 🎯 Как работает функционал

### Загрузка страницы

1. `page.tsx` (Server Component) загружается на сервере
2. Компоненты (Client Components) монтируются в браузере
3. `useTransition` показывает loading состояние
4. ServerActions запрашивают данные с сервера

### Deposit/Withdraw

```
Клик на кнопку
  ↓
Вызов ServerAction handleDeposit/handleWithdraw
  ↓
Motion анимирует кнопку (whileTap={{ scale: 0.95 }})
  ↓
Toast уведомление
  ↓
Перезагрузка баланса через getWalletBalance
  ↓
NumberFlow анимирует новое значение
```

### График с кешированием

```
Выбор timeframe
  ↓
Вызов getChartData(publicKey, timeframe)
  ↓
На сервере:
  - Проверка кеша `${publicKey}:chart:${timeframe}`
  - Если есть - возврат из кеша (TTL 60 сек)
  - Если нет - генерация данных + сохранение в кеш
  ↓
Отправка данных клиенту
  ↓
ChartView отрисовывает Area Chart
  ↓
При наведении:
  - onHover обновляет состояние
  - NumberFlow анимирует число
  - Дата обновляется
```

## 🎨 Motion Анимации

Все кнопки и интерактивные элементы имеют:

```typescript
whileHover={{ scale: 1.05, translateY: -2 }}
whileDrag={{ scale: 0.96 }}
whileTap={{ scale: 0.95 }}
transition={{ type: "spring", stiffness: 400, damping: 10 }}
```

Карточки загружаются с:

```typescript
initial={{ opacity: 0, y: 20 }}
animate={{ opacity: 1, y: 0 }}
transition={{ duration: 0.5 }}
```

## 📱 Responsive Design

- ✅ **Мобильные (320px+)** - 1 column
- ✅ **Планшеты (768px+)** - 2 columns
- ✅ **Десктоп (1024px+)** - 2 columns с широким layout

## 🚀 Команды

```bash
npm run dev      # Dev сервер (http://localhost:3000)
npm run build    # Production build
npm start        # Production сервер
npm run lint     # Lint проверка

# Очистить кеш Next.js
rm -rf .next

# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install
```

## 🔍 Отладка

**Если не отображается:**

1. Проверьте `.env.local` файл
2. Убедитесь что dev сервер запущен (http://localhost:3000)
3. Откройте консоль браузера (F12) - ошибки клиента
4. Посмотрите terminal - ошибки сервера
5. Очистите кеш: `rm -rf .next` и перезапустите

**Если ошибки сервера:**

1. Проверьте API ключи в `.env.local`
2. Убедитесь что EtherScan API доступен
3. Проверьте RPC URL доступность

## ✅ Чек-лист ТЗ

- ✅ **Next.js + TypeScript** - no errors
- ✅ **2 основных блока** - My Wallet (левый) + Profit/Loss (правый)
- ✅ **NumberFlow** - анимирует все числа
- ✅ **Motion** - whileHover, whileDrag, whileTap на кнопках
- ✅ **EtherScan** - получение цены, баланса, транзакций
- ✅ **Deposit/Withdraw** - полностью рабочий функционал
- ✅ **P&L отслеживание** - без USDC (используется WETH)
- ✅ **Server Actions** - все запросы через "@use server" функции
- ✅ **Кеширование** - 1 минута с привязкой к publicKey
- ✅ **Все ключи в .env** - правильная конфигурация
- ✅ **Client/Server разделение** - page.tsx Server, компоненты Client
- ✅ **Интерактивный график** - наведение меняет значение выше

## 📊 Компоненты

| Компонент              | Описание                                    | Тип    |
| ---------------------- | ------------------------------------------- | ------ |
| **BalanceCard**        | Левый блок: баланс, Deposit/Withdraw        | Client |
| **ChartBlock**         | Правый блок: P&L график, timeframe          | Client |
| **ChartView**          | Area Chart с Recharts и интерактивностью    | Client |
| **MotionButton**       | Кнопка с Motion анимациями                  | Client |
| **TimeframeSelector**  | Выбор временного диапазона (1H-All)         | Client |
| **PortfolioStats**     | Карточки статистики (Total, 24h, High, Low) | Client |
| **TransactionHistory** | История недавних транзакций                 | Client |
| **PageHeader**         | Заголовок страницы с описанием              | Client |
| **ToasterProvider**    | Toast notifications (react-hot-toast)       | Client |

## 🔐 Безопасность

- ✅ **Private Key** хранится ТОЛЬКО в `.env.local` (не коммитится)
- ✅ **Server Actions** - все операции с кошельком на сервере
- ✅ **Public Key** доступен клиенту только для фильтрации
- ✅ **API ключи** защищены окружением
- ✅ **EtherScan API** используется безопасно

## 📝 Структура компонента

```tsx
"use client"; // Client Component

import { useState, useTransition } from "react";
import { motion } from "framer-motion";
import NumberFlow from "@number-flow/react";
import { someAction } from "@/actions/...";

export function MyComponent({ publicKey }: Props) {
  const [data, setData] = useState(0);
  const [isPending, startTransition] = useTransition();

  // Загрузка данных
  useEffect(() => {
    startTransition(async () => {
      const result = await someAction(publicKey);
      setData(result);
    });
  }, [publicKey]);

  // Motion анимация + NumberFlow
  return (
    <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }}>
      <NumberFlow value={data} />
    </motion.div>
  );
}
```

## 📞 Полезные ссылки

- [IMPLEMENTATION.md](IMPLEMENTATION.md) - детальная документация
- [CHECKLIST.md](CHECKLIST.md) - проверка соответствия ТЗ
- [Next.js Doc](https://nextjs.org/docs)
- [Framer Motion Doc](https://www.framer.com/motion/)
- [Etherscan API](https://docs.etherscan.io/)

---

**Приложение полностью готово к использованию!** 🚀
